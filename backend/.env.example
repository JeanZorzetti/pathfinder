# ========================================
# PATHFINDER BACKEND - ENVIRONMENT VARIABLES
# ========================================

# Application Settings
NODE_ENV=development                    # development | production | test
PORT=3001                               # Porta da API
API_PREFIX=api/v1                       # Prefixo das rotas

# ========================================
# CORS - Cross-Origin Resource Sharing
# ========================================
FRONTEND_URL=http://localhost:5173      # URL do frontend local (Vite default)
VERCEL_FRONTEND_URL=https://pathfinder.vercel.app  # URL do frontend em produção

# ========================================
# DATABASE - PostgreSQL
# ========================================
# Para desenvolvimento local:
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USER=pathfinder
DATABASE_PASSWORD=your_secure_password_here
DATABASE_NAME=pathfinder_db

# Para produção (Easypanel):
# DATABASE_HOST=dados_pathpost          # Nome interno do serviço no Easypanel
# DATABASE_HOST=31.97.23.166           # OU IP externo da VPS
# DATABASE_PORT=5432
# DATABASE_USER=PathPost
# DATABASE_PASSWORD=sua_senha_segura_do_easypanel
# DATABASE_NAME=PathPost

DATABASE_SSL=false                      # true em produção se usar SSL

# ========================================
# REDIS CACHE (OPCIONAL)
# ========================================
# Redis é opcional - o app usa cache em memória por padrão
# Descomente para usar Redis em produção:
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_PASSWORD=
# REDIS_TTL=3600                        # TTL padrão em segundos

# ========================================
# JWT AUTHENTICATION
# ========================================
# IMPORTANTE: Gere secrets fortes para produção!
# Use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"

JWT_SECRET=your_super_secure_jwt_secret_change_this_in_production_min_32_chars
JWT_EXPIRATION=7d                       # 7 dias = 1 semana

JWT_REFRESH_SECRET=your_different_super_secure_refresh_secret_change_in_prod
JWT_REFRESH_EXPIRATION=30d              # 30 dias = 1 mês

# ========================================
# SECURITY
# ========================================
# Bcrypt Salt Rounds (maior = mais seguro, mais lento)
BCRYPT_SALT_ROUNDS=10                   # 10 é bom balanço para produção

# Rate Limiting (proteção contra brute force)
RATE_LIMIT_TTL=60                       # Janela de tempo em segundos
RATE_LIMIT_MAX=100                      # Máximo de requests por TTL

# ========================================
# MONITORING & LOGGING (OPCIONAL)
# ========================================
# LOG_LEVEL=info                        # error | warn | info | debug
# SENTRY_DSN=                           # Para error tracking
# NEW_RELIC_LICENSE_KEY=                # Para APM
